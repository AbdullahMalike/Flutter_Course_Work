import 'dart:math';

void main() {
  num base = 2, exponent = 3;
  num result = pow(base, exponent);
  print(
      "value of base is: $base and exponent is: $exponent and the final answer is: $result");
}
